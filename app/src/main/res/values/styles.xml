<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="ToolBar">
        <item name="android:editTextColor">@android:color/white</item>
    </style>

    <style name="CustomAlertDialogStyle" parent="android:Theme.Holo.Light.Dialog">
        <item name="android:background">@drawable/bg_custom_progress</item>
        <item name="android:windowBackground">@color/transparent</item>
    </style>

<!--   for toolbar : color change-->
    <style name="ToolbarColoredBackArrow" parent="AppTheme">
        <item name="android:textColorSecondary">#fff</item>
    </style>

<!--    <style name="ToolBarPopUpTheme" parent="@android:style/Widget.PopupMenu">-->
<!--        <item name="android:popupBackground">@android:color/black</item>-->
<!--    </style>-->
    <style name="ToolBarPopUpTheme" parent="ThemeOverlay.AppCompat.Light">
<!--        <item name="android:colorBackground">@color/colorHeader</item>-->
        <item name="android:textColor">@color/colorHeader</item>
    </style>



</resources>
